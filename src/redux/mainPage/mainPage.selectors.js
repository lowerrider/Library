export const selectSearchValue = (state) => state.mainPage.searhValue;
export const selectCategory = (state) => state.mainPage.category;
export const selectSort = (state) => state.mainPage.sort;
export const selectPage = (state) => state.mainPage.page;
export const selectIsLoadingBookCards = (state) =>
  state.mainPage.isLoadingBookCards;
